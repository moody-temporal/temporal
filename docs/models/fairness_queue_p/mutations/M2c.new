    if (kept > 0) {
      // if we have any tasks at all in memory, readLevel is the max of that set
      readLevel = merged[kept - 1];
    } else {
      // M2c: collapse readLevel to ackLevel when the merged set is empty
      readLevel = ackLevel;
    }
